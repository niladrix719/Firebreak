{
  "mcpServers": {
    "firebreak": {
      "command": "npx",
      "args": ["tsx", "src/mcp/server.ts"],
      "env": {
        "DATABASE_PATH": "./data/firebreak.db",
        "LOG_LEVEL": "silent"
      }
    }
  }
}
